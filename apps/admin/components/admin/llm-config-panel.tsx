/**
 * Re-export shim — the panel was split into focused modules under
 * ./llm-config/. This file keeps existing imports working without changes.
 */
export { LlmConfigPanel } from "./llm-config";
export type { RegistryModel } from "./llm-config";
